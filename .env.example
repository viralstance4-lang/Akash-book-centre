# Copy this file to .env (untracked, see .gitignore) for `docker compose up`.
# docker-compose.yml reads POSTGRES_PASSWORD from here for the Postgres service
# and to build the backend's DATABASE_URL.
POSTGRES_PASSWORD=
